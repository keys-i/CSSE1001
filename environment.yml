# ------------------------------------------------------------------------------
# Project: CSSE1001
# Description: My CSSE1001 assignments written in python
# Maintainer: Radhesh Goel (Keys-I)
# Created: 2022-05-12
# Python: 3.10
# Notes:
# - Keep versions loosely pinned unless you need strict reproducibility.
# - Use `conda env export --from-history` to capture only explicit deps later.
# ------------------------------------------------------------------------------
name: csse1001

channels:
  - conda-forge

dependencies:
  - python=3.10
  - pip
  - pip:
      - black==22.10.0
      - flake8==6.1.0
